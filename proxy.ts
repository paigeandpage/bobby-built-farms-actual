// Clerk authentication has been disabled for the public site takeover.
// Account/admin authentication will be replaced separately under the new owner.
export default function proxy() {}

export const config = {
  matcher: [],
};
